
商品一覧  <%= link_to new_admin_item_path do %>

 <div><i class="fas fa-plus-circle fa-lg"></i></div>
<% end %>


<main>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <div class="col-md-3">

        <table>
            <thead>
            <tr>
              <th>商品ID</th>
              <th>商品名</th>
              <th>税抜価格</th>
            </tr>
            </thead>
           <tbody>
            <% @items.each do |item| %>
            <tr>
            <td><%= item.id %></td>
            <td><%= link_to item.name,edit_admin_item_path(item.id) %></td>
            <td><%= item.price %></td>
            </tr>
            <% end %>
           </tbody>
        </table>
</main>