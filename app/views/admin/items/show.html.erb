<h1>商品詳細</h1>

<table>
<tr>
    <%= image_tag @item.get_profile_image(100,100) %>
</tr>
  <tr> 
  <td>商品名</td>
  <td><%= @item.name %></td>
  </tr>
  
  <tr>
    <td>商品説明</td>
  　<td><%= @item.introduction %></td>
  </tr>
  
   <tr>
     <td>税込価格<br>(税抜価格)</td>
     <td><%= @item.price %></td>
   </tr>
</table>

<%= button_to "編集する" ,edit_admin_item_path(@item.id),method: :get,class:"btn btn-sm btn-success" %>