<h2>Log in1</h2>
ログイン<br>
会員の方はこちらからログイン
<%= form_with model: @costomer do |f| %>
  <div class="field">
    <%= f.label :メールアドレス %>
    <%= f.email_field :email, autofocus: true %>
  </div>

  <div class="field">
    <%= f.label :パスワード%>
    <%= f.password_field :password, autocomplete: "current-password" %>
  </div>

  <% if devise_mapping.rememberable? %>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :remember_me %>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit "ログイン" %>
  </div>
  会員登録がお済みでない方
  <%= link_to "こちら", new_customer_registration_path(@customer.id) %>からログインしてください。
<% end %>

<%#= render "publics/shared/links" %>
